pipeline {
    agent any
    stages {
        stage('Print servers') {
            steps {
                script {
                    def servers = ['srv1', 'srv2', 'srv3']
                    for (server in servers) {
                        echo "Deploying to ${server}"
                    }
                }
            }
        }
    }
}
